/**
 * Created by pablo on 20/06/2015.
 */
public class TestPercolation {
/*

    public static void main(String[] args){
        Stopwatch timer = new Stopwatch();
        int N = 3000;
        int iter = 0;
        Percolation per = new Percolation(N);
        while (!per.percolates()){
            int i = 1 + (int) ((double)N * Math.random());
            int j = 1 + (int) ((double)N * Math.random());
            per.open(i,j);
            iter++;
        }
        double tTime = timer.elapsedTime();

        System.out.println( "Tiempo: " + tTime  );
        System.out.println( (double)per.open/(N*N) );

    }

*/
}
